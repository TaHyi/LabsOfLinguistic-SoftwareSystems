@model IEnumerable<FamLab00004.Models.BankAccount>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Current_Balance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Account_Type.Account_Type1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customer.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Current_Balance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Account_Type.Account_Type1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Account_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Account_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Account_ID })
        </td>
    </tr>
}

</table>
