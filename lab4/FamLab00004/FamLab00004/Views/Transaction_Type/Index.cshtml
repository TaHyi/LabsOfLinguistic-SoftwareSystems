@model IEnumerable<FamLab00004.Models.Transaction_Type>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Transaction_Type1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Transaction_Type1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Transaction_Type_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Transaction_Type_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Transaction_Type_ID })
        </td>
    </tr>
}

</table>
