@model IEnumerable<FamLab00004.Models.Card>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Card_Reg_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BankAccount.Account_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Card_Type.Card_Type1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Card_Reg_Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BankAccount.Account_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Card_Type.Card_Type1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Card_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Card_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Card_ID })
        </td>
    </tr>
}

</table>
